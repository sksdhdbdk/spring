/*
public class WeaponFactoryBuilder {
    public static WeaponFactory get(String factoryType){
        if ("TankFactory".equals(factoryType)) {
            return new TankFactory();
        } else if ("DaggerFactory".equals(factoryType)) {
            return new DaggerFactory();
        }else {
            throw new RuntimeException("不支持该工厂的生产");
        }

    }
}
*/
